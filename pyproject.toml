[project]
name = "neogrc"
authors = [{ name = "Neotec Integrated Solutions", email = "support@neotec.ai" }]
description = "Evidence-driven GRC engineering for Frappe/ERPNext v15"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"
erpnext = ">=15.0.0,<16.0.0"

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.format]
indent-style = "tab"

[tool.ruff.lint]
select = ["F", "E", "W", "I", "UP", "B", "RUF", "S"]
ignore = ["E501", "B904", "S101"]

[tool.ruff.lint.per-file-ignores]
"**/test_*.py" = ["S"]
